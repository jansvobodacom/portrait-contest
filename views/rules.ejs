<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pravidla – <%= settings.contest_title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;1,400&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    :root{--bg:<%= settings.color_bg||'#fafaf8' %>;--surface:#fff;--text:<%= settings.color_primary||'#1a1a18' %>;--text2:#5a5955;--text3:#9a9893;--border:rgba(0,0,0,0.1);--accent:<%= settings.color_accent||'#0F6E56' %>;--r:4px}
    body{font-family:'Inter',sans-serif;background:var(--bg);color:var(--text);min-height:100vh}
    a{color:inherit;text-decoration:none}
    nav{display:flex;align-items:center;justify-content:space-between;padding:1.25rem 2.5rem;border-bottom:0.5px solid var(--border);background:var(--surface);position:sticky;top:0;z-index:100}
    .nav-logo{font-family:'Playfair Display',serif;font-size:17px}
    .nav-logo em{font-style:italic;font-weight:400;color:var(--text2)}
    .nav-links{display:flex;gap:2rem}
    .nav-links a{font-size:12px;color:var(--text2);letter-spacing:.6px;text-transform:uppercase;transition:color .2s}
    .nav-links a:hover,.nav-links a.active{color:var(--text)}
    .container{max-width:640px;margin:0 auto;padding:3rem 2rem}
    .page-title{font-family:'Playfair Display',serif;font-size:32px;font-weight:400;letter-spacing:-1px;margin-bottom:2rem}
    .rules-content{font-size:15px;line-height:1.85;color:var(--text);white-space:pre-wrap}
    footer{display:flex;align-items:center;justify-content:space-between;padding:1.5rem 2.5rem;border-top:0.5px solid var(--border);background:var(--surface);margin-top:3rem}
    .footer-logo{font-family:'Playfair Display',serif;font-size:14px}
    .footer-links{display:flex;gap:1.5rem}
    .footer-links a{font-size:12px;color:var(--text2)}
  </style>
</head>
<body>
<nav>
  <a href="/" class="nav-logo">Jan Svoboda <em>Photography</em></a>
  <div class="nav-links">
    <a href="/">Přihlásit se</a>
    <a href="/galerie">Galerie</a>
    <a href="/vysledky">Výsledky</a>
    <a href="/pravidla" class="active">Pravidla</a>
    <% if (typeof isAdmin !== 'undefined' && isAdmin) { %><a href="/admin">Admin</a><% } %>
  </div>
</nav>
<div class="container">
  <h1 class="page-title">Pravidla soutěže</h1>
  <div class="rules-content"><%= settings.rules_text || 'Pravidla budou brzy doplněna.' %></div>
</div>
<footer>
  <div class="footer-logo">Jan Svoboda Photography</div>
  <div class="footer-links"><a href="/galerie">Galerie</a><a href="/admin/login">Admin</a></div>
</footer>
</body>
</html>
